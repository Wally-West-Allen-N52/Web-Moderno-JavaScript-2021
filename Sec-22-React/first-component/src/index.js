import React from 'react';
import ReactDOM from 'react-dom';

// importt new component "First" from Componente folder 
import First from './Components/First';

ReactDOM.render (
  <First />, // component reference
  document.getElementById('root')
)
